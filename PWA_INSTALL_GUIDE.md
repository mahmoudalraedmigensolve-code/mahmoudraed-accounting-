# 📱 دليل تثبيت التطبيق (PWA)

## ما هو PWA؟
Progressive Web App (PWA) هو تطبيق ويب يمكن تثبيته على الهاتف أو الكمبيوتر ويعمل مثل التطبيقات العادية، مع إمكانية العمل بدون إنترنت (offline).

---

## 📲 كيفية التثبيت على الموبايل

### أندرويد (Chrome):

1. **افتح الموقع في متصفح Chrome**
   - اذهب إلى عنوان الموقع

2. **اضغط على القائمة (⋮)**
   - القائمة الموجودة في أعلى يمين المتصفح

3. **اختر "Add to Home screen" أو "إضافة إلى الشاشة الرئيسية"**
   - سيظهر لك نافذة تأكيد

4. **اضغط "Add" أو "إضافة"**
   - سيتم إضافة أيقونة التطبيق على الشاشة الرئيسية

5. **افتح التطبيق من الشاشة الرئيسية**
   - الآن سيفتح كتطبيق مستقل بدون شريط عنوان المتصفح!

### iPhone (Safari):

1. **افتح الموقع في Safari**

2. **اضغط على زر المشاركة**
   - الأيقونة الموجودة في الأسفل (مربع مع سهم لأعلى)

3. **اسحب لأسفل واختر "Add to Home Screen"**
   - أو "إضافة إلى الشاشة الرئيسية"

4. **اضغط "Add" في أعلى اليمين**

5. **افتح التطبيق من الشاشة الرئيسية**

---

## 💻 كيفية التثبيت على الكمبيوتر

### Chrome / Edge:

1. **افتح الموقع**

2. **ستظهر أيقونة تثبيت في شريط العنوان**
   - أيقونة (+) أو أيقونة كمبيوتر

3. **اضغط على الأيقونة**

4. **اضغط "Install" أو "تثبيت"**

5. **سيفتح التطبيق في نافذة منفصلة**

---

## ✨ مميزات التطبيق بعد التثبيت

✅ **يفتح كتطبيق مستقل** - بدون شريط عنوان المتصفح
✅ **أيقونة على الشاشة الرئيسية** - سهولة الوصول
✅ **تجربة استخدام أفضل** - يبدو وكأنه تطبيق أصلي
✅ **يعمل بدون إنترنت** - (بعد أول زيارة)
✅ **تحديثات تلقائية** - يتحدث تلقائيًا عند فتحه

---

## 🔧 للمطورين - كيفية اختبار PWA محليًا

1. **شغل السيرفر في Production Mode**:
   ```bash
   npm run build
   npm start
   ```

2. **افتح الموقع على `localhost:3000`**

3. **افتح Chrome DevTools**:
   - اضغط F12
   - اذهب إلى تاب "Application" أو "Lighthouse"
   - اختبر PWA من "Manifest" و "Service Workers"

4. **اختبار PWA Score**:
   - في Lighthouse، اختر "Progressive Web App"
   - اضغط "Generate report"

---

## 🎨 تخصيص الأيقونات

إذا أردت تغيير الأيقونات:

1. افتح `generate-icons.html` في المتصفح
2. سيتم عرض الأيقونات بأحجام مختلفة
3. احفظ كل أيقونة:
   - كليك يمين → Save image as...
   - `icon-192.png` → حجم 192×192
   - `icon-512.png` → حجم 512×512
4. ضع الملفات في مجلد `/public`

أو شغل:
```bash
node scripts/generate-pwa-icons.js
```

---

## 📝 ملاحظات مهمة

⚠️ **لازم يكون الموقع على HTTPS** (أو localhost للتطوير)
⚠️ **Service Worker يشتغل فقط في Production Mode**
⚠️ **بعد التعديلات، امسح الـ Cache من إعدادات المتصفح**

---

## 🆘 مشاكل شائعة وحلولها

### المشكلة: "Add to Home Screen" مش ظاهر

**الحل**:
- تأكد إن الموقع على HTTPS
- تأكد إن ملف `manifest.json` موجود ويعمل
- تأكد إن الأيقونات موجودة في `/public`
- جرب في Private/Incognito Mode

### المشكلة: التطبيق بيفتح في Chrome مش standalone

**الحل**:
- تأكد إن `"display": "standalone"` موجودة في `manifest.json`
- امسح التطبيق المثبت وثبته تاني
- تأكد إن Service Worker شغال

### المشكلة: التطبيق مش بيشتغل offline

**الحل**:
- تأكد إن Service Worker مسجل صح
- افتح DevTools → Application → Service Workers
- شوف إذا كان في أخطاء

---

## 📚 موارد إضافية

- [PWA Documentation](https://web.dev/progressive-web-apps/)
- [Service Workers Guide](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)
- [Web App Manifest](https://developer.mozilla.org/en-US/docs/Web/Manifest)
